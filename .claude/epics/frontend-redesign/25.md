---
name: Theme System and Layout Redesign
status: open
created: 2026-03-18T14:58:28Z
updated: 2026-03-18T15:08:56Z
github: https://github.com/Temirlaaan/lms-informatika/issues/25
depends_on: [22]
parallel: true
conflicts_with: []
---

# Task: Theme System and Layout Redesign

## Description
Create the ThemeProvider context for dark/light theme switching, redesign both StudentLayout and TeacherLayout with modern sidebar navigation using lucide-react icons, and add theme toggle. Update App.tsx to wrap with ThemeProvider.

## Acceptance Criteria
- [ ] ThemeProvider context created with light/dark/system modes
- [ ] Theme persisted to localStorage key `theme`
- [ ] System preference auto-detected on first visit via `prefers-color-scheme`
- [ ] Theme toggle button in both sidebar layouts (sun/moon icon)
- [ ] StudentLayout redesigned: modern sidebar with lucide icons, clean header, smooth transitions
- [ ] TeacherLayout redesigned: same modern treatment
- [ ] PublicLayout updated for dark mode support
- [ ] App.tsx wrapped with ThemeProvider
- [ ] `<html>` class toggled between `light` and `dark`
- [ ] All layout elements use CSS variables (no hardcoded colors)

## Technical Details
- ThemeProvider: ~40 line React Context with `useEffect` for class toggle and localStorage
- Sidebar icons: Home, BookOpen, GraduationCap, BarChart3, User, Settings, LogOut (from lucide-react)
- Active nav item: use primary color background with foreground text
- Mobile sidebar: use Sheet component from Task 24 (if available), otherwise keep current approach
- Keep existing route structure unchanged

## Files Affected
- `src/components/ThemeProvider.tsx` (new)
- `src/components/layout/StudentLayout.tsx` (rewrite)
- `src/components/layout/TeacherLayout.tsx` (rewrite)
- `src/components/layout/PublicLayout.tsx` (update)
- `src/App.tsx` (add ThemeProvider wrapper)

## Effort Estimate
- Size: L
- Hours: 3-4
- Parallel: true (with Tasks 23, 24)

## Definition of Done
- [ ] Theme toggles correctly between light and dark
- [ ] Theme persists across page refreshes
- [ ] Both layouts look modern with consistent styling
- [ ] `npx tsc -b` passes
