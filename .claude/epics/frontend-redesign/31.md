---
name: Loading States, Animations, and Final QA
status: open
created: 2026-03-18T14:58:28Z
updated: 2026-03-18T15:08:56Z
github: https://github.com/Temirlaaan/lms-informatika/issues/31
depends_on: [30]
parallel: false
conflicts_with: []
---

# Task: Loading States, Animations, and Final QA

## Description
Add Skeleton loading states to all data-fetching pages, implement CSS hover animations on cards, add page transition effects, clean up any remaining raw Tailwind patterns, and perform final QA across both themes.

## Acceptance Criteria
- [ ] Every page that fetches data shows Skeleton placeholders during loading (not spinners)
- [ ] Card hover effects: subtle scale (1.02) + shadow transition
- [ ] Smooth transitions on theme toggle (CSS transition on background-color, color)
- [ ] No hardcoded color values remaining (all use CSS variables)
- [ ] No conflicting CSS classes (e.g., `inline-block flex`)
- [ ] Dark mode verified on every single page (18 pages)
- [ ] Light mode verified on every single page
- [ ] `npx tsc -b` passes with zero errors
- [ ] Docker build succeeds
- [ ] No console errors or warnings in browser

## Technical Details
- Skeleton: match approximate layout shape (e.g., Skeleton cards for dashboard, Skeleton rows for tables)
- Theme transition: add `transition-colors duration-200` to body or main container
- Cleanup: search for remaining raw color classes like `bg-blue-600`, `text-white`, `bg-gray-*` and replace with semantic CSS variable classes
- Final check: run `npx tsc -b` and `docker compose build` to verify

## Files Affected
- All 18 page files (Skeleton loading + cleanup)
- `src/index.css` (animations, transitions)
- `Dockerfile` / `docker-compose.yml` (build verification)

## Effort Estimate
- Size: M
- Hours: 2-3
- Parallel: false (final task)

## Definition of Done
- [ ] Skeleton loading on all pages
- [ ] Animations smooth and consistent
- [ ] Zero TypeScript errors
- [ ] Docker build passes
- [ ] Both themes verified on all pages
