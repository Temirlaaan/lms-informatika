---
name: Project Setup & All Models
status: open
created: 2026-03-03T10:48:32Z
updated: 2026-03-03T12:06:31Z
github: https://github.com/Temirlaaan/lms-informatika/issues/2
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Project Setup & All Models

## Description
Initialize both Django backend and React frontend projects. Create all 12 data models across 4 Django apps (accounts, courses, quizzes, grades), run migrations, and register models in Django Admin. Set up the Vite + React + TypeScript + Tailwind frontend with proper folder structure.

## Acceptance Criteria
- [ ] Django project initialized with config/ directory (settings, urls, wsgi)
- [ ] PostgreSQL configured (DB: lms_informatika, User: lms_user)
- [ ] Django settings: LANGUAGE_CODE='kk', TIME_ZONE='Asia/Almaty', CORS for localhost:5173
- [ ] 4 Django apps created: accounts, courses, quizzes, grades
- [ ] Custom User model (AbstractUser) with role, full_name, avatar, grade_class, created_at
- [ ] courses models: Section, Topic, Lesson, LessonImage, TopicProgress
- [ ] quizzes models: Quiz, Question, Choice, QuizAttempt, StudentAnswer
- [ ] grades models: Grade (with grade_value calculation)
- [ ] All migrations run successfully
- [ ] All models registered in Django Admin
- [ ] Vite + React 18 + TypeScript project initialized
- [ ] Tailwind CSS configured with primary (#2563EB) and accent (#10B981) colors
- [ ] Frontend folder structure: api/, components/, pages/, hooks/, context/, types/, utils/
- [ ] requirements.txt with all Python dependencies
- [ ] package.json with all frontend dependencies

## Technical Details
- Backend: Python 3.12, Django 5, DRF, simplejwt, django-cors-headers, Pillow
- Frontend: React 18, TypeScript, Vite, Tailwind CSS, React Router v6, Axios
- Custom User must be set before first migration (AUTH_USER_MODEL = 'accounts.User')
- MEDIA_ROOT configured for file uploads

## Dependencies
- [ ] PostgreSQL running locally
- [ ] Python 3.12 environment
- [ ] Node.js / npm installed

## Effort Estimate
- Size: M
- Parallel: false (must complete before all other tasks)

## Definition of Done
- [ ] `python manage.py migrate` runs cleanly
- [ ] Django dev server starts without errors
- [ ] Vite dev server starts without errors
- [ ] All 12 models visible in Django Admin
